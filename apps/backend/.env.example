DATABASE_URL="postgresql://postgres:postgres@localhost:5432/bookflow?schema=public"
PORT=3001

# Upstream BookStack base url for proxying non-migrated modules
BOOKSTACK_BASE_URL="http://localhost:8081"

# Module migration flags (true → handled by TS API, false → proxy to BookStack)
FLAG_BOOKS=true
FLAG_PAGES=false
FLAG_COMMENTS=false
