PORT=3001

# Upstream BookStack base url for proxying non-migrated modules
BOOKSTACK_BASE_URL="http://localhost:8081"

# PostgreSQL connection string for Prisma
# When set and reachable, DB-backed routes like /health and /books will be enabled
# Example: postgresql://postgres:postgres@localhost:5432/bookflow?schema=public
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/bookflow?schema=public"

# Migration flags are hardcoded in src/config.ts for phase 1; no runtime env flags currently.
